# StoryForge 視覺小說遊戲引擎系統分析與重構建議

## 目錄
1. [系統概述](#系統概述)
2. [技術選型分析與建議](#技術選型分析與建議)
3. [未完成功能分析與實作建議](#未完成功能分析與實作建議)
4. [系統Bug與效能問題分析](#系統bug與效能問題分析)
5. [重構建議與實施計劃](#重構建議與實施計劃)

---

## 系統概述

### 當前架構
- **開發語言**: Visual Basic .NET
- **目標框架**: .NET 7.0 Windows
- **UI框架**: Windows Forms
- **專案類型**: 桌面應用程式（Windows專用）
- **程式碼量**: 主程式約1,598行

### 核心功能模組
1. **劇本解析系統**: 支援自訂腳本指令解析
2. **多媒體播放**: 圖片、音樂、音效、影片（未完成）
3. **UI渲染**: 角色立繪、對話框、特效
4. **資源管理**: 壓縮檔載入、圖片格式轉換
5. **使用者互動**: 全螢幕切換、拖拽載入

### 支援的腳本指令
- `background`: 背景圖片
- `music`: 背景音樂（循環播放）
- `sound`: 音效
- `txt`: 對話文字
- `character`: 角色立繪（left/center/right）
- `title_fullscreen`: 全螢幕標題
- `description`: 描述文字
- `location`: 地點標示
- `fade_white/fade_black/flash`: 淡入淡出效果
- `hide_left/hide_right/hide_center`: 隱藏角色
- `stopmusic`: 停止背景音樂
- `video`: **未實作**
- `exit`: 結束章節

---

## 技術選型分析與建議

### 當前技術棧的優劣勢

#### 優勢
1. **開發效率高**: VB.NET語法簡單，適合快速原型
2. **Windows整合**: 與Windows系統深度整合
3. **成熟的多媒體支援**: LibVLCSharp提供強大的媒體播放能力

#### 劣勢
1. **平台限制**: 僅支援Windows，無法跨平台
2. **效能瓶頸**: Windows Forms不適合高效能圖形渲染
3. **維護性差**: 1,598行單一檔案，缺乏模組化
4. **社群支援有限**: VB.NET在遊戲開發領域資源較少
5. **多執行緒管理**: 大量使用`Thread.Sleep`和`Task.Run`，容易產生競態條件

### 推薦的遊戲專用技術選型

#### 方案一：Unity（強烈推薦）
**適用場景**: 需要跨平台、高品質的視覺小說引擎

**優勢**:
- ✅ 跨平台支援（Windows/macOS/Linux/Android/iOS/Web）
- ✅ 豐富的視覺小說資產商店（對話系統、存檔系統）
- ✅ 強大的2D渲染引擎和動畫系統
- ✅ 內建Scene管理和資源管理系統
- ✅ 龐大的社群支援和教學資源
- ✅ 可使用C#（比VB.NET更現代）

**技術棧建議**:
```
語言: C#
引擎: Unity 2022 LTS
UI框架: Unity UI (uGUI) 或 UI Toolkit
對話系統: Yarn Spinner 或 Ink
存檔系統: JsonUtility 或 BinaryFormatter
音訊: Unity Audio Mixer
```

**第三方套件推薦**:
- **Fungus**: 開源視覺小說框架
- **Yarn Spinner**: 對話系統
- **DOTween**: 高效能動畫
- **Easy Save**: 強大的存檔系統

#### 方案二：Godot Engine
**適用場景**: 需要完全開源、輕量級的解決方案

**優勢**:
- ✅ 完全開源免費
- ✅ 輕量級（安裝包<50MB）
- ✅ 跨平台支援
- ✅ 內建場景系統和節點架構
- ✅ GDScript語法簡單易學
- ✅ 2D渲染效能優異

**技術棧建議**:
```
語言: GDScript 或 C#
引擎: Godot 4.x
UI框架: Control節點系統
對話系統: Dialogue Manager插件
```

#### 方案三：Ren'Py（最適合視覺小說）
**適用場景**: 專注於視覺小說，不需要複雜遊戲機制

**優勢**:
- ✅ 專為視覺小說設計
- ✅ 內建存檔、回溯、快速跳過等功能
- ✅ 簡單的腳本語法
- ✅ 跨平台支援
- ✅ 大量成功商業案例

**技術棧建議**:
```
語言: Ren'Py Script (Python-based)
引擎: Ren'Py 8.x
```

#### 方案四：Web技術棧（現代化方案）
**適用場景**: 需要Web部署、最大化可訪問性

**優勢**:
- ✅ 真正的跨平台（任何有瀏覽器的設備）
- ✅ 易於分發和更新
- ✅ 現代前端生態系統

**技術棧建議**:
```
語言: TypeScript
框架: React + Canvas/WebGL
遊戲引擎: Phaser 3 或 PixiJS
UI框架: React + Tailwind CSS
狀態管理: Redux 或 Zustand
構建工具: Vite
部署: Vercel/Netlify
```

### 推薦優先順序
1. **Ren'Py** - 如果專注於視覺小說類型
2. **Unity** - 如果需要擴展性和專業品質
3. **Web技術** - 如果需要最大可訪問性
4. **Godot** - 如果需要完全開源

---

## 未完成功能分析與實作建議

### 1. Video指令實作

#### 當前狀態
```vb
Case "video"
    cmdQueue.Enqueue(
        Function() As Object
            'I have no time to implement this
            Return 0
        End Function)
```

#### 問題分析
- 程式碼中僅保留空殼，無實際功能
- LibVLCSharp已整合，但未用於video指令

#### 實作建議（VB.NET版本）
```vb
Case "video"
    cmdQueue.Enqueue(
        Function() As Object
            Dim videoPath = chapterPath + "\" + cmd.Split(" ")(1)
            Dim videoControl As New Panel()
            videoControl.Dock = DockStyle.Fill
            videoControl.BackColor = Color.Black
            
            Dim videoView As New VideoView()
            videoView.Dock = DockStyle.Fill
            videoControl.Controls.Add(videoView)
            
            Me.Controls.Add(videoControl)
            videoControl.BringToFront()
            
            Dim media = New Media(libvlc, videoPath)
            Dim videoPlayer = New MediaPlayer(media)
            videoPlayer.EnableHardwareDecoding = True
            videoView.MediaPlayer = videoPlayer
            
            ' 播放完成後移除控制項
            AddHandler videoPlayer.EndReached, Sub()
                Me.Invoke(Sub()
                    videoPlayer.Stop()
                    videoPlayer.Dispose()
                    Me.Controls.Remove(videoControl)
                    videoControl.Dispose()
                End Sub)
            End Sub
            
            videoPlayer.Play()
            Return 0
        End Function)
```

#### 進階功能建議
- 支援字幕檔（.srt）
- 視訊控制：暫停、快轉、跳過
- 視訊完成自動繼續劇情

### 2. 音量與語言設定

#### 當前問題
- 音量硬編碼為70: `mp.Volume = 70`（第284行）
- 無設定UI介面
- 無多語言支援

#### 實作建議

**設定資料結構**:
```vb
Public Class GameSettings
    Public Property MasterVolume As Integer = 70
    Public Property MusicVolume As Integer = 100
    Public Property SoundVolume As Integer = 100
    Public Property Language As String = "zh-TW"
    Public Property TextSpeed As Integer = 50
    
    Public Sub Save()
        Dim json = JsonSerializer.Serialize(Me)
        File.WriteAllText(GameSettingsPath, json)
    End Sub
    
    Public Shared Function Load() As GameSettings
        If File.Exists(GameSettingsPath) Then
            Dim json = File.ReadAllText(GameSettingsPath)
            Return JsonSerializer.Deserialize(Of GameSettings)(json)
        End If
        Return New GameSettings()
    End Function
End Class
```

**設定UI介面**:
- 加入設定按鈕到主選單
- 使用TrackBar控制音量
- ComboBox選擇語言
- 即時預覽音量變化

**語言系統**:
```vb
Public Class Localization
    Private Shared strings As Dictionary(Of String, String)
    
    Public Shared Sub LoadLanguage(languageCode As String)
        Dim path = $"{gamePath}localization\{languageCode}.json"
        Dim json = File.ReadAllText(path)
        strings = JsonSerializer.Deserialize(Of Dictionary(Of String, String))(json)
    End Sub
    
    Public Shared Function Get(key As String) As String
        Return If(strings.ContainsKey(key), strings(key), key)
    End Function
End Class
```

### 3. 顯示目前時間

#### 實作建議
```vb
' 在Form1中加入時鐘Label
Dim clockLabel As New dbLabel
clockLabel.Font = New Font(pfc.Families(0), 12, FontStyle.Regular)
clockLabel.ForeColor = Color.White
clockLabel.BackColor = Color.FromArgb(100, 0, 0, 0)
clockLabel.AutoSize = True
clockLabel.Location = New Point(10, 10)

' 啟動計時器
Dim clockTimer As New Timer()
clockTimer.Interval = 1000
AddHandler clockTimer.Tick, Sub()
    clockLabel.Text = DateTime.Now.ToString("HH:mm:ss")
End Sub
clockTimer.Start()

Me.Controls.Add(clockLabel)
clockLabel.BringToFront()
```

### 4. 儲存遊玩進度

#### 當前問題
- 無存檔功能
- 關閉遊戲後進度遺失
- 無法回到先前選擇點

#### 實作建議

**存檔資料結構**:
```vb
Public Class SaveData
    Public Property SaveTime As DateTime
    Public Property ChapterPath As String
    Public Property ScriptIndex As Integer
    Public Property BackgroundImage As String
    Public Property Characters As Dictionary(Of String, String)
    Public Property CurrentMusic As String
    Public Property ScreenshotPath As String
    
    Public Sub Save(slotNumber As Integer)
        ' 截圖當前畫面作為存檔縮圖
        Dim screenshot = New Bitmap(Me.ClientSize.Width, Me.ClientSize.Height)
        Me.DrawToBitmap(screenshot, New Rectangle(0, 0, Me.ClientSize.Width, Me.ClientSize.Height))
        ScreenshotPath = $"{gamePath}saves\slot{slotNumber}_thumb.png"
        screenshot.Save(ScreenshotPath)
        
        ' 序列化存檔資料
        Dim json = JsonSerializer.Serialize(Me, New JsonSerializerOptions With {.WriteIndented = True})
        File.WriteAllText($"{gamePath}saves\slot{slotNumber}.json", json)
    End Sub
    
    Public Shared Function Load(slotNumber As Integer) As SaveData
        Dim path = $"{gamePath}saves\slot{slotNumber}.json"
        If File.Exists(path) Then
            Dim json = File.ReadAllText(path)
            Return JsonSerializer.Deserialize(Of SaveData)(json)
        End If
        Return Nothing
    End Function
End Class
```

**存檔UI設計**:
- ESC鍵開啟遊戲選單
- 提供3-5個存檔槽位
- 顯示存檔時間和縮圖
- 快速存檔/讀檔快捷鍵（F5/F9）

**自動存檔**:
```vb
' 在每個章節結束時自動存檔
Case "exit"
    ' 自動存檔到特殊槽位
    Dim autoSave As New SaveData With {
        .SaveTime = DateTime.Now,
        .ChapterPath = chapterPath,
        .ScriptIndex = deQCount
    }
    autoSave.Save(0) ' 槽位0保留給自動存檔
```

---

## 系統Bug與效能問題分析

### 嚴重問題（High Priority）

#### 1. 競態條件（Race Condition）
**位置**: 多處使用`Task.Run`和共享狀態

**問題**:
```vb
' Line 1548: displayStory函數
If isDisplayingEffect Then
    stopEffect = True
    Exit Sub
Else
    ' 多個執行緒可能同時修改cmdQueue
    Do While cmdQueue.Count
        Select Case cmdQueue(0)()
```

**風險**:
- 多執行緒同時訪問`cmdQueue`
- `isDisplayingEffect`、`stopEffect`未使用鎖定機制
- 可能導致程式崩潰或行為異常

**解決方案**:
```vb
' 使用鎖定機制
Private ReadOnly cmdQueueLock As New Object()

SyncLock cmdQueueLock
    If cmdQueue.Count > 0 Then
        cmdQueue.Dequeue()
    End If
End SyncLock
```

#### 2. 記憶體洩漏
**問題區域**:
- 圖片資源未正確釋放（Line 991）
- MediaPlayer物件累積（Line 271-289）

**具體問題**:
```vb
' 每次載入圖片都建立新的Image物件，但舊的未Dispose
Me.BackgroundImage = Drawing.Image.FromFile(img)
```

**解決方案**:
```vb
' 正確釋放資源
If Me.BackgroundImage IsNot Nothing Then
    Dim oldImage = Me.BackgroundImage
    Me.BackgroundImage = Nothing
    oldImage.Dispose()
End If
Me.BackgroundImage = Drawing.Image.FromFile(img)
```

#### 3. 單一檔案過大
**問題**: Form1.vb包含1,598行程式碼，違反單一職責原則

**影響**:
- 難以維護和除錯
- 測試困難
- 程式碼複用性低

**重構建議**: 參見第5章

### 中等問題（Medium Priority）

#### 4. Thread.Sleep濫用
**位置**: 整個檔案共25處使用

**問題**:
```vb
Thread.Sleep(100)  ' 阻塞UI執行緒
```

**影響**:
- 浪費CPU資源
- UI可能凍結
- 無法精確控制時序

**解決方案**:
```vb
' 使用async/await替代Thread.Sleep
Await Task.Delay(100)

' 或使用Timer
Dim timer = New Timer()
timer.Interval = 100
AddHandler timer.Tick, AddressOf OnTimerTick
timer.Start()
```

#### 5. 錯誤處理不完整
**問題**:
```vb
' Line 800-818: 只捕獲IOException和InvalidDataException
Try
    ZipFile.ExtractToDirectory(zipPath, extractPath, big5, True)
Catch ex As Exception
    Select Case ex.GetType()
        Case GetType(IOException)
            ' 處理
        Case GetType(InvalidDataException)
            ' 處理
    End Select
End Try
```

**缺失**:
- 未處理UnauthorizedAccessException
- 未處理PathTooLongException
- 圖片載入失敗未處理

**改善建議**:
```vb
Try
    ' 操作
Catch ex As IOException
    Logger.Error("IO錯誤", ex)
    ShowErrorDialog("磁碟讀寫錯誤")
Catch ex As UnauthorizedAccessException
    Logger.Error("權限錯誤", ex)
    ShowErrorDialog("沒有足夠權限")
Catch ex As Exception
    Logger.Error("未預期錯誤", ex)
    ShowErrorDialog("發生未預期錯誤，請查看日誌")
End Try
```

#### 6. 圖片格式轉換效率低
**問題**:
```vb
' Line 172-175: 每次都轉換，未使用快取
Public Function imgConverter(img As String)
    Dim p = converted_imgPath + Path.GetFileNameWithoutExtension(img) + ".jpg"
    ImageExtensions.SaveAsJpeg(Image.Load(img), p)
    Return p
End Function
```

**改善**:
```vb
' 檢查快取
Public Function imgConverter(img As String) As String
    Dim cachedPath = converted_imgPath + Path.GetFileNameWithoutExtension(img) + ".jpg"
    If File.Exists(cachedPath) Then
        Return cachedPath
    End If
    
    ImageExtensions.SaveAsJpeg(Image.Load(img), cachedPath)
    Return cachedPath
End Function
```

### 輕微問題（Low Priority）

#### 7. 硬編碼的魔術數字
**範例**:
```vb
Line 292: Me.ClientSize = New Drawing.Size(Screen.PrimaryScreen.Bounds.Width * 0.98, Screen.PrimaryScreen.Bounds.Width * 0.42)
Line 284: mp.Volume = 70
Line 699: title_fullscreen.Width = Me.ClientSize.Width * 0.7
```

**建議**: 使用常數或設定檔
```vb
Private Const DefaultVolume As Integer = 70
Private Const WindowWidthRatio As Double = 0.98
Private Const WindowHeightRatio As Double = 0.42
Private Const TitleWidthRatio As Double = 0.7
```

#### 8. 註解過少
**問題**: 複雜邏輯缺乏註解說明

**建議**: 加入XML文件註解
```vb
''' <summary>
''' 處理角色立繪的顯示邏輯，支援左、中、右三個位置
''' </summary>
''' <param name="imgPath">角色圖片路徑</param>
''' <param name="location">顯示位置（left/center/right）</param>
Public Sub ShowCharacter(imgPath As String, location As String)
    ' 實作...
End Sub
```

#### 9. 缺乏日誌系統
**問題**: 除錯困難，無法追蹤問題

**建議**: 整合日誌框架
```vb
Imports NLog

Public Class Form1
    Private Shared ReadOnly Logger As Logger = LogManager.GetCurrentClassLogger()
    
    Private Sub LoadStory()
        Logger.Info("開始載入故事")
        Try
            ' 載入邏輯
            Logger.Info("故事載入成功")
        Catch ex As Exception
            Logger.Error(ex, "故事載入失敗")
        End Try
    End Sub
End Class
```

### 效能優化建議

#### 1. 圖片預載入
```vb
' 在背景預先載入即將使用的圖片
Private imageCache As New Dictionary(Of String, Image)

Private Async Sub PreloadImages(chapterPath As String)
    Await Task.Run(Sub()
        For Each cmd In scriptLs
            If cmd.StartsWith("background") Or cmd.StartsWith("character") Then
                Dim imgPath = chapterPath + "\" + cmd.Split(" ")(1)
                If Not imageCache.ContainsKey(imgPath) Then
                    imageCache(imgPath) = Image.FromFile(imgPath)
                End If
            End If
        Next
    End Sub)
End Sub
```

#### 2. 音訊串流優化
```vb
' 使用較小的緩衝以降低延遲
mp.FileCaching = 300  ' 從10改為300ms，提升流暢度
```

#### 3. UI更新批次處理
```vb
' 避免頻繁更新UI
Private Sub UpdateUIBatch()
    Me.SuspendLayout()
    ' 多個UI更新
    title_fullscreen.Text = newText
    txt.Text = newTxt
    Me.ResumeLayout()
End Sub
```

---

## 重構建議與實施計劃

### 架構重構

#### 目標架構（如繼續使用VB.NET）
```
StoryForge/
├── Core/                       # 核心功能
│   ├── ScriptParser.vb        # 劇本解析器
│   ├── CommandExecutor.vb     # 指令執行器
│   ├── ResourceManager.vb     # 資源管理
│   └── SaveManager.vb         # 存檔管理
├── UI/                         # 使用者介面
│   ├── MainMenuForm.vb        # 主選單
│   ├── GameForm.vb            # 遊戲畫面
│   ├── SettingsForm.vb        # 設定介面
│   └── SaveLoadForm.vb        # 存讀檔介面
├── Media/                      # 多媒體
│   ├── AudioPlayer.vb         # 音訊播放器
│   ├── VideoPlayer.vb         # 視訊播放器
│   └── ImageRenderer.vb       # 圖片渲染
├── Models/                     # 資料模型
│   ├── ScriptCommand.vb       # 腳本指令模型
│   ├── GameSettings.vb        # 遊戲設定
│   └── SaveData.vb            # 存檔資料
└── Utils/                      # 工具類
    ├── Logger.vb              # 日誌系統
    └── Extensions.vb          # 擴展方法
```

#### 分離後的ScriptParser範例
```vb
Public Class ScriptParser
    Private ReadOnly supportedCommands As New HashSet(Of String) From {
        "background", "music", "txt", "sound", "character", 
        "fade_white", "fade_black", "flash", "video", "exit"
    }
    
    Public Function Parse(scriptPath As String) As List(Of ScriptCommand)
        Dim commands As New List(Of ScriptCommand)
        Dim lines = File.ReadAllLines(scriptPath)
        
        For Each line In lines
            If String.IsNullOrWhiteSpace(line) Then Continue For
            
            Dim parts = ParseLine(line)
            If Not supportedCommands.Contains(parts.CommandName) Then
                Throw New InvalidScriptException($"不支援的指令: {parts.CommandName}")
            End If
            
            commands.Add(New ScriptCommand With {
                .Name = parts.CommandName,
                .Arguments = parts.Arguments
            })
        Next
        
        Return commands
    End Function
    
    Private Function ParseLine(line As String) As (CommandName As String, Arguments As List(Of String))
        ' 解析邏輯...
    End Function
End Class
```

### 測試策略

#### 單元測試範例（使用xUnit）
```vb
Imports Xunit

Public Class ScriptParserTests
    <Fact>
    Public Sub Parse_ValidScript_ReturnsCommands()
        ' Arrange
        Dim parser As New ScriptParser()
        Dim scriptPath = "test_script.txt"
        File.WriteAllText(scriptPath, "background test.jpg" & vbCrLf & "music bgm.mp3")
        
        ' Act
        Dim commands = parser.Parse(scriptPath)
        
        ' Assert
        Assert.Equal(2, commands.Count)
        Assert.Equal("background", commands(0).Name)
        Assert.Equal("music", commands(1).Name)
    End Sub
    
    <Fact>
    Public Sub Parse_InvalidCommand_ThrowsException()
        ' Arrange
        Dim parser As New ScriptParser()
        Dim scriptPath = "invalid_script.txt"
        File.WriteAllText(scriptPath, "invalid_command test")
        
        ' Act & Assert
        Assert.Throws(Of InvalidScriptException)(Sub() parser.Parse(scriptPath))
    End Sub
End Class
```

### 遷移到Unity的實施計劃

如果決定遷移到Unity，建議的步驟：

#### Phase 1: 準備階段（1-2週）
1. 學習Unity基礎（官方教程）
2. 研究Unity視覺小說框架（Fungus/Yarn Spinner）
3. 設計新的專案架構
4. 建立Git分支

#### Phase 2: 核心功能遷移（3-4週）
1. **場景系統**
   - 建立主選單場景
   - 建立遊戲場景
   - 設定場景切換

2. **劇本系統**
   ```csharp
   // Unity C# 版本
   public class ScriptParser : MonoBehaviour
   {
       public List<ScriptCommand> ParseScript(string scriptPath)
       {
           var commands = new List<ScriptCommand>();
           var lines = File.ReadAllLines(scriptPath);
           
           foreach (var line in lines)
           {
               commands.Add(ParseCommand(line));
           }
           
           return commands;
       }
   }
   ```

3. **UI系統**
   - 使用TextMeshPro處理文字
   - Canvas設計對話框
   - 實作打字機效果

4. **資源管理**
   - 使用Addressables系統
   - 實作資源預載入
   - ZIP檔解壓與載入

#### Phase 3: 進階功能（2-3週）
1. 存檔系統
2. 設定介面
3. 音訊管理器
4. 視訊播放器整合

#### Phase 4: 優化與測試（2週）
1. 效能分析
2. 記憶體優化
3. 自動化測試
4. 用戶測試

### 漸進式重構策略（維持VB.NET）

如果不想完全重寫，建議漸進式重構：

#### 階段1: 解耦合（立即執行）
1. 提取ScriptParser類別
2. 提取ResourceManager類別
3. 提取AudioPlayer類別

#### 階段2: 加入中介層（1-2週）
1. 實作Command Pattern處理劇本指令
2. 實作Event System處理UI更新
3. 加入Dependency Injection

#### 階段3: 完善功能（2-3週）
1. 實作存檔系統
2. 加入設定介面
3. 完成video指令

#### 階段4: 效能優化（1週）
1. 修復多執行緒問題
2. 實作資源快取
3. 優化圖片載入

### 品質保證檢查清單

#### 程式碼品質
- [ ] 所有類別符合單一職責原則
- [ ] 公開API都有XML文件註解
- [ ] 錯誤都有適當的Exception處理
- [ ] 沒有硬編碼的魔術數字
- [ ] 使用async/await替代Thread.Sleep

#### 功能完整性
- [ ] video指令完整實作
- [ ] 存檔/讀檔功能正常
- [ ] 音量設定可調整並儲存
- [ ] 多語言切換功能
- [ ] 顯示時鐘

#### 效能指標
- [ ] 啟動時間 < 3秒
- [ ] 章節載入時間 < 2秒
- [ ] 記憶體使用 < 500MB
- [ ] CPU使用率 < 30%
- [ ] 無記憶體洩漏

#### 使用者體驗
- [ ] 所有操作都有視覺回饋
- [ ] 錯誤訊息清晰易懂
- [ ] 支援鍵盤快捷鍵
- [ ] 自動存檔功能
- [ ] 流暢的動畫效果

---

## 總結

### 關鍵建議

1. **技術選型**：
   - 短期：維持VB.NET，進行模組化重構
   - 中長期：遷移到Unity或Ren'Py

2. **優先事項**：
   - 修復競態條件（高風險）
   - 實作存檔系統（使用者最需要）
   - 完成video指令（功能完整性）

3. **效能改善**：
   - 實作圖片快取機制
   - 替換Thread.Sleep為async/await
   - 修復記憶體洩漏

4. **架構改善**：
   - 將Form1.vb拆分為多個類別
   - 實作SOLID原則
   - 加入日誌系統

### 預估工作量

| 項目 | 預估時間 | 優先級 |
|------|---------|--------|
| 修復競態條件 | 2-3天 | 高 |
| 記憶體洩漏修復 | 1-2天 | 高 |
| 實作存檔系統 | 5-7天 | 高 |
| Video指令實作 | 2-3天 | 中 |
| 設定介面開發 | 3-4天 | 中 |
| 架構重構 | 2-3週 | 中 |
| Unity遷移 | 6-8週 | 低 |

### 最終建議

鑑於專案當前狀態和視覺小說遊戲的特性，**強烈建議遷移到Ren'Py或Unity**：

- **Ren'Py**適合快速開發，內建所有視覺小說必備功能
- **Unity**適合需要更多擴展性和商業化的專案

如果必須維持VB.NET：
1. 立即修復高優先級Bug
2. 實作存檔系統（使用者體驗關鍵）
3. 進行模組化重構（維護性）

無論選擇哪種方案，建議逐步遷移，確保每個階段都有可用的版本，降低風險。
