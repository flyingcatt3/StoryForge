// Localization module placeholder
pub struct Localization;

impl Localization {
    pub fn new() -> Self {
        Self
    }
}

impl Default for Localization {
    fn default() -> Self {
        Self::new()
    }
}
